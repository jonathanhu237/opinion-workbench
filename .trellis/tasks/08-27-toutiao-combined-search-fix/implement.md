# Execution Plan

## Approval and Ownership

- [x] User explicitly approved creation and implementation without another approval turn.
- [x] Main created PRD and reviewed current source/specs plus one bounded unmodified live run.
- [x] Final scope is delayed-render readiness hardening with an honest unresolved historical cause.
- [x] Curate implement/check context, validate artifacts, then activate under the delegated approval.

## Implementation

1. Dispatch one implementer for only the derivative client and parser regression tests. Main keeps
   ownership of all live data, browser, runtime, docs/specs, remote validation and Git operations.
2. Add the delayed-render regression first. Synchronize just the test to Centaurus and demonstrate
   its failure with the unchanged client; preserve this red evidence before proceeding.
3. Add the bounded coherent-pending readiness loop and focused safety/lifecycle tests. No parser,
   URL, database, worker-protocol or frontend changes without a newly demonstrated need.
4. Synchronize the exact local source/tests to Centaurus, run focused and complete maintained
   MediaCrawler tests, then the backend checks. Use existing mise-managed tooling; install tools
   through mise only if needed. Do not copy runtime, credentials, `.git` or local dependencies.
5. Independently review code and task evidence; resolve mechanical/local issues and rerun affected
   checks. Review must distinguish proven synthetic failure from an unproven historical root cause.

## Live Acceptance

- [x] Fresh private local SQLite backup; formal rule 1 unchanged and all prior operations terminal.
- [x] One task-only rule (ID 5); unmodified combined run 43 completed with two new results.
- [x] Reload the owned idle lightweight backend/worker after offline gates to load modified source.
- [x] One combined product run (limit 5), then at most one identical repeat and one object-only
  baseline. No other platforms or model requests. Blockers stop their flow without retries.
- [x] Validate observed stable-ID reuse and honest new/repeated counts; no overlap is inconclusive.
- [x] Delete only temporary rule 5 after terminal checks. Verify formal rule 1, old history and
  first-seen values, database integrity/FKs, no active work, and user-tab preservation.
- [x] Record tests, real outcomes, remaining limitations and spec changes; no commit/push/archive.

## Check Commands

Centaurus repository: `/home/jonathanhu237/code/longtian-public-opinion-management`.
Derivative: `.venv/bin/pytest -q tests` from `third_party/MediaCrawler`; use a focused parser test
selection for the red/green loop. Backend: existing `uv run --frozen ruff check .`,
`uv run --frozen pytest` from `backend` under the managed runtime.
Local task context validation and `git diff --check` in both repositories are also required.
No frontend source changes are planned; earlier UI changes and their tests are separate work.

The remote machine has no system/browser-cache Chromium. Main provisioned browser tooling through
mise (`@puppeteer/browsers` and `extract-zip`) for a separate test-only browser cache. Parser fixtures
accept `TEST_CHROMIUM_EXECUTABLE` only when the explicit file exists and is executable; no production
browser discovery changed. Pass the verified test binary for every parser/full-suite invocation.

## Authorized Delivery Follow-up

The user subsequently requested commit and push. The original no-Git implementation boundary above
records what happened during implementation, not a prohibition on this newly authorized delivery.

1. Confirm frozen source hashes, existing independent review, remote regression gates, clean index
   and unchanged remote tips. Check only this task's staged files for accidental sensitive data.
2. Commit and push the two derivative files on its existing `main` branch.
3. Stage only the parent gitlink, two backend specs and this task directory. Verify mode 160000,
   public HTTPS URL, clean derivative, remotely reachable SHA and a fresh recursive clone of that
   proposed parent snapshot before committing/pushing parent `main`.
4. Verify both remote heads. Leave other dirty files untouched and keep this task unarchived.
