# Authorized Real-Video Cost Probe — 2026-08-28

## Approval and boundary

After discussing a 3–5-video cost/quality sample, the user explicitly said to use
already collected results. Reuse this verification-only task for that follow-up;
do not activate the planned production enrichment or summary children. This
addendum supersedes the historical synthetic-text-only limit for this probe only.
No new searches, rule edits, collection writes, product implementation, commits,
or scheduled/automatic model calls are authorized.

## Bounded procedure

- Select up to three usable existing video posts, including a likely local issue,
  an ambiguous-location item and, if accessible, an apparently unrelated item.
  Inspect at most five candidate originals. Selection is qualitative, not a random
  benchmark and not a precision/recall estimate.
- Use the existing Chrome binding and owned inspection tabs, preserving user tabs.
  Inspect rendered text and actual video-element media. Do not read browser
  storage, cookies, hidden application state, account profiles or comments.
  Authentication and platform challenges stop the affected flow for manual action.
- Use only observed actual self-contained MP4 media, at most 6 MiB and 90 seconds
  each. No conversion, frame-only replacement, silent clipping, alternate private
  endpoint, new signing algorithm, or uploaded URL containing platform credentials.
  Missing/unsupported/oversized inputs are explicit unsuccessful samples.
- Keep media and credentials local. Reuse the existing saved configuration service
  and AI client security boundary; never copy the key from conversation. Do not
  change the endpoint/model or print secrets. Existing saved public metadata is
  revision 1, qwen3.5-omni-plus, Alibaba compatible-mode HTTPS endpoint.
- Make at most three media calls total, one per usable video, with inline bytes,
  text-only output, max_tokens 1200, a 180-second deadline and no automatic retry.
  Add documented stream_options.include_usage in the isolated probe transport;
  reuse the product DNS/TLS/redirect/error/SSE handling. Capture numeric usage only,
  never request headers, inline payloads, provider errors or hidden reasoning.
- Use the saved monitoring scope and source date. Ask for related/irrelevant/uncertain,
  a brief attributed summary, and concrete visual/audio observations to distinguish
  video understanding from title paraphrase. Treat model output as untrusted data.
- No final composition call is needed for this experiment. Report token counts and
  a public-list-price estimate separately from actual billing/discounts. Explain
  uncertainty and any evidence the model failed to understand.

## Ownership and verification

Main owns browser/media/provider operations and sanitized evidence. A native Trellis
implementer may create only a task-local standalone probe and fake-transport tests;
it must not access runtime data, credentials or real providers. Heavy checks run on
Centaurus after one-way source sync; the small live client remains local under the
previous approved local credential/browser-runtime exception. If a standalone
service is used, stop the idle production backend during live AI operations so
there is only one credential-operation owner; restore the existing application.
An independent checker reviews the harness and sanitized results without repeating
paid calls. Preserve all existing unrelated changes in both repositories.

## Within-budget diagnostic correction

The first invocation attempted only content 370 and returned valid stream/usage,
but the final text failed the probe's local JSON/schema validation. Its answer was
not retained, so the exact format mismatch is unknown. Do not retry that video.
Correct only the isolated probe to retain bounded secret-checked final answer text
as private plain data and distinguish local parsing/schema failures from provider
failures. An exact whole-answer JSON fence may be removed deterministically; do not
repair semantics or relax the analysis schema. Local output-format failure may
continue to the next unique sample; provider/network failures still stop the run.
After offline tests/review, invoke only the original remaining IDs 63 and 372 once
each, using a separate manifest/output. Total requests remain at most 1 + 2 = 3.
This is not a retry, extra sample, configuration change or production feature.
