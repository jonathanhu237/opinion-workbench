---
status: accepted
---

# 发现去重与条目处理策略分离

搜索阶段负责发现、去重和入库，分析失败不会使一个已发现条目重新成为新增。后续处理独立从内容库按所选策略选取条目，选择包含失败项时覆盖库中所有当前仍失败的条目，不要求它们出现在本次搜索结果里。该选择将采集批次与分析候选解耦，保留独立的发现记录和处理结果；手动流程的选材与启动边界遵循 ADR-0008，选项默认值及具体重试限制另行确定。
