# 快手、小红书、今日头条搜索采集修复

Status: resolved

## 问题与范围

用户发现批次 11 的快手、小红书、今日头条均为 0 条，要求检查并修复。

- 小红书：DOM 刚加载时解析为 0，等待 3 秒后可识别 30 条；原逻辑提前确认搜索词完成。
- 快手：当前搜索结果为 `.video-list .photo-card`，没有普通内容链接。公开封面地址的 `clientCacheKey` 含对应视频编号，已通过打开相应 `/short-video/<id>` 核对标题一致。
- 今日头条：综合搜索结果使用 `/search/jump` 和公开落地链接封装，含头条文章、微头条和外站结果；原直链解析规则无法识别。采用已核查的「资讯」入口获取头条文章，仍支持解析遇到的视频与微头条公开链接。

## 验收

- 三个平台等待渲染；无可识别结果且没有明确空态时，返回未识别状态，不写入成功空结果检查点。
- 快手仅从已识别的搜索卡片读取公开编号、标题、作者与点赞数，不下载媒体或读取页面私有状态。
- 今日头条展开已知的公开链接包装，规范化文章、视频和微头条链接；拒绝外站内容与非法地址。
- 小红书识别搜索卡片标题、作者、日期和点赞数；无标题的图文不使用作者信息或页面标题填充正文。
- 维持每词条数上限、去重、批次进度及原有微博、抖音行为。
- 浏览器仍连接且页面仍存在时，初次导航/DOM 快照失败最多重试一次；计入原页数与时间预算，在写入任何记录之前完成重试。
- 用原监控规则新建三平台验证批次，核对采集、入库和结果展示。

## 验证记录

- 2026-09-08：相关后端测试 166 项通过，Ruff 检查通过。
- 第一轮应用验证批次 12 暴露了快手编号长度约束错误（已修正为当前 15 位），以及头条综合页部分搜索词仅返回外站的问题（已切换资讯入口）。
- 最终真实应用验证批次 13，三个平台、五个搜索词、每词上限 10 条：快手 32 条，小红书 31 条，今日头条 46 条（44 条新增、2 条再次命中），全部五词完成，批次状态 completed。已核对结果 API 每页返回 5 条及小红书真实弹窗显示。
- 小红书首次导航出现 browser_unavailable，手动重试后完成全部搜索词。随后补充有限自动重试，保留超时、断开连接及持续错误的失败状态。
- 最新代码真实验证批次 14：快手 34 条、小红书 30 条、今日头条 46 条，三个平台均在一次采集尝试内完成五个搜索词，无需手动恢复。
- 最新相关测试 171 项通过；Ruff 与 diff 检查通过。全量测试 1086 通过、90 失败；独立导出 HEAD 基线在同一环境运行得到 1060 通过、相同的 90 项失败，失败集合完全一致。后续新增的四项有限重试测试已包含在 171 项通过的相关测试中。
