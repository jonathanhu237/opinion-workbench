Status: resolved

# 修复已有数据库的报告创建与历史读取

用户在报告向导提交 186 条选材后遇到“暂时无法读取或保存分析数据，请稍后重试”。

## 原因

- v18 的数据库触发器把默认提示词绑定到当时的版本 ID；v34 增加文字分析模板后未更新触发器，已有数据库拒绝新默认模板，事务回滚。
- 历史报告投影把保存的旧默认模板与当前模板比较，错误拒绝读取已有报告。

## 修复

- v37 迁移只更新分析任务和自动任务的提示词插入/更新约束，以当前默认模板为准；保留引用、阶段、模式和自动任务目标的一致性检查。
- 已结束分析任务读取冻结的历史提示词，仍校验内容哈希、结构和冲突。执行快照仍要求默认模板匹配当前版本。
- 不修改历史任务、报告、提示词的冻结内容。

## 验证

- 真实数据库副本复现 SQLite `analysis job prompt choice is invalid`；升级后同样的 186 条选材创建成功，重放返回同一 ID，9 条旧报告可读。
- 50 项针对测试通过，涵盖升级、幂等初始化、冻结历史、旧默认版本不得作为新默认执行、报告生成和自动任务。
- 先备份本地数据库，再重启后端自动升级至 v37。
- 本地历史接口 HTTP 200；通过用户页面保存的同一个请求重试，创建报告任务 #11，186 条内容进入处理，已打开进度面板。
- 已确认第一条模型分析完成、第二条继续处理。
- 完整后端测试 1098 passed / 90 failed；失败集合与此前独立基线完全一致，无新增失败。
