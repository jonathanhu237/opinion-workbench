# 采集批次结果弹窗

Status: resolved

## 已确定的交互

- 采集批次详情页底部不再内嵌采集结果列表。
- 保留各平台卡片的「查看结果」，点击后打开结果弹窗。
- 弹窗展示当前批次、所选平台的结果，汇总该平台多次尝试后的去重内容。
- 结果分页展示，每页 5 条。
- 沿用现有结果信息和「全部／新增／再次命中」筛选，弹窗标题标明平台与结果总数。
- 关闭弹窗后返回原采集进度页面。
- 报告界面继续作为全部历史内容的查看及选材入口。
- 本次尝试详情保留现有行为。

## 验收要点

- 点击不同平台的「查看结果」时展示对应平台的批次结果，不串用上一平台的数据。
- 分页每页至多 5 条，能访问末页；切换平台或筛选时回到第一页。
- 保留结果加载、空结果、错误提示，以及弹窗关闭和键盘操作能力。
- 批次页原有采集进度、数量统计和异常恢复操作保持可用。

## Comments

- 2026-09-08：用户将移除底部列表的方案调整为点击「查看结果」打开分页弹窗，并明确每页 5 条。讨论完成，等待明确实施请求。
- 本次为可逆的界面调整，不引入新领域术语或架构决策，因此不新增 ADR 或修改 CONTEXT.md。

## 实施与审查

- 2026-09-08：用户验收后明确要求提交并推送。

- 2026-09-08：按用户明确请求，由 Luna Max 执行 implement-without-review；主代理直接执行第一轮 Standards / Spec 审查，两方面均无发现。未提交 Git，等待用户验收。
- 审查基线：`7a201c6ec067a933227510ad39537b720d53ac7a`，与当前工作区改动比较。
- 相关测试 66/66 通过；类型检查、Lint、生产构建通过。
- 全量前端测试 558/561 通过。`collection-runs.test.tsx` 的开始微博采集参数断言和首次采集空态文案断言在基线副本同样失败；`App.test.tsx` 在全量运行中失败，随后在基线和当前代码的定向复跑均通过。
- 真实本地 Chrome 验证：底部列表移除；抖音 39 条结果前两页各 5 条；切换平台从第一页开始；微博 13 条结果末页 3 条且禁用下一页；空筛选、关闭后焦点返回原按钮正常。已检查弹窗截图布局。
