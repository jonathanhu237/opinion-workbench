Status: resolved

# 完整跑通真实报告生成链路

用户要求：亲自尝试生成报告，修复链路中的 bug，完整跑通后再汇报。

## 执行范围

- 继续当前真实报告任务 #11，分析任务 #14，共 186 条用户已选内容；复用用户保存的选材、两阶段默认提示词和当前模型配置。
- 验证内容补全、逐条文字分析、相关性判断、分章节汇总、报告生成与界面查看。
- 遇到程序问题先复现、修复并验证；保留已完成内容与历史，不伪造完成，不以采集摘要替代缺失原文。
- 如需重试，复用成功的单条总结并防止重复创建同一请求。
- 在本机运行、验证与管理 Git；本轮不提交代码。

## 已有前置修复

- 报告选材接受缺省互动统计。
- v37 迁移修复新默认提示词被旧数据库触发器拒绝，旧报告读取保留原提示词快照。

## 进度

已成功创建任务，9 条内容完成文字分析，其余仍在处理。完成标准为生成可读的最终报告并在实际页面验证。

### 真实运行发现与修复

- 抖音详情 DOM 尚未加载便冻结为空文本：改为有界等待配文，支持 video→note 重定向期间的短暂 DOM 失效。
- 小红书规范详情链接返回 HTTP 200 的软 404：识别不可用页面，并从匹配内容 ID 的原搜索卡片进入详情，访问参数只保留在浏览器内存。
- 快手隐藏播放器错误被误判为平台限流：排除隐藏节点，以作品配文容器为正文边界，避免评论、操作控件混入。
- 保存的 unavailable 文本导致后续新报告永远无法重新获取：新选材保留重新获取机会，成功分析继续复用。
- HTML 注释尾文本节点触发 AttributeError，整批被记为中断：跳过非元素节点属性检查，仍检查其父元素可见性；添加安全的异常类型和代码位置诊断。

任务 #11 已取消；#12/#13 保留中断历史；当前 #14（分析 #17）复用 51 条成功结果，继续原始 186 条选材。没有修改历史状态或重复调用已成功条目的模型分析。

验证：真实抖音、快手、小红书、头条详情读取通过；新增/相关测试 69 项通过。全量测试（注释节点修复前）1105 项通过、90 项失败，失败集合与修复前基线一致。最终报告尚待完成。

### 第二轮完整链路验证

- 小红书部分笔记仅出现在“最新”排序的后续卡片窗口，详情回查增加有界最新排序与继续加载。真实失败样本已恢复 502 字正文。
- 小红书合法的“标题非空、描述为空”笔记不应等待正文超时；要求详情标题与描述容器实际加载，再使用现有文字，图片内容仍不推断。
- 报告复用成功分析时遗漏 `analysis_input_version`，导致 147 条复用结果被报告误标 `stale_evidence`。修复复制当前输入版本，回归测试断言复用的所有条目均成为报告可用证据。
- 发现本机 DNS 间歇返回 `198.18.1.229`，原公网校验正确拒绝发送请求；增加最多 3 次短暂 DNS 重试，仅针对全部返回 benchmarking 虚拟地址的情况，不向这些地址发送凭据、不放宽公网校验。持续虚拟地址和回环地址仍被拒绝。
- 当前任务 #19（分析 #22、报告 #18）：184 条文字分析全部复用，报告 ready=184、unavailable=2，已进入 judging。
- 两条小红书缺口为结果 #313 / #318：规范链接软 404，原搜索/最新/加深回查仍未出现匹配 ID；#318 额外标题搜索仍无匹配。仅说明暂不能获取，不推断删除，不伪造原文。
- 最新全量测试（最后的版本复制/DNS 改动前）：1109 passed / 90 failed，失败集合与原基线完全一致。最后两个改动的定向测试 103 passed，Ruff 与 diff check 通过。

### 最终总览稳定性修复

- 仅对相同主机端口，在 60 秒内复用先前已验证的公网解析结果，以兼容本机代理间歇返回的全 benchmarking 地址；不向虚拟地址或内网地址发请求，缓存不续期。
- DashScope Qwen3.8 Max 的 JSON 请求显式设置 JSON object 输出及独立思考预算，避免默认长思考挤占请求超时。
- 总览限选主要事件，控制长度，详细材料保留原子节；总览使用独立引擎版本，重试不重跑已成功的逐条判断与详细分析。
- 实际报告 #23 已完成，但最终页面检查发现模型正文含半截句子；增加完整句子要求和明显截断内容拒绝检查，继续真实重试而未交付该报告。

### DeepSeek 接入与最终验收（用户明确要求继续）

- 用户提供 DeepSeek 密钥并授权试用；同一总览输入的独立对比测试使用 deepseek-v4-pro，16.9 秒返回 15 个完整段落，格式/引用校验通过，但同一楼盘重复成段。
- 应用配置已通过正常设置 API 切换至 DeepSeek V4 Pro（revision 4），密钥沿用应用加密存储，未写入源码或测试。
- DeepSeek 官方接口使用 thinking.type=disabled 与 JSON object 输出，移除不属于其文本协议的 modalities 字段；扩展仅作用于已验证的官方主机和模型。
- 本机 fake-IP DNS 阻止应用连接，增加仅针对官方 DeepSeek/DashScope 主机且全部 DNS 答案为 benchmarking 地址的有界 HTTPS DNS 回查。固定 TLS 公网解析器只接收主机名；取得的地址仍必须全为公网，并固定连接 IP、验证原主机 TLS。未知主机、内网或混合 DNS 答案保持拒绝。应用内 DeepSeek 连接测试通过。
- 总览先核对项目全称/简称、地点与经过，再按事件合并引用，延期/验收/交付按时间串联到同一段。
- 报告聚合失败保留第一个节点的具体错误，避免无效 JSON/格式/引用统统显示为 internal_error。
- 当前真实报告 #28 使用原始 186 条选材（184 条已保存文字证据、2 条缺口），复用第一阶段原文与总结，由 DeepSeek 重新判断相关性及生成报告。
- 定向验证：AI 客户端 119 项通过；报告生命周期 25 项通过（含三个阶段保留具体失败原因）。全量测试与实际报告继续运行，尚未宣称完成。

验收进度补充：
- 前端类型检查通过，报告阅读/材料说明/API 选材的 27 项测试通过。
- 全量后端测试运行结果 1144 passed / 89 failed；与修复前 88 个失败逐项比对，仅新增一个旧断言仍要求 internal_error。已更新该冒烟用例为实际 invalid_citations，并连同生命周期测试重新验证，共 31 项通过。
- 原有 88 个失败保留于 baseline-failures.md，未删除旧测试或宣称全量通过。
- 真实报告 #28 在结果 #244 处遇到一次 ai_provider_unavailable，其余继续处理。待本批完成后仅重试失败节点，成功节点保留复用。

- 报告 #29 复用 183 条判断并成功补完一条连接失败，最终判断为 49 相关 / 31 无关 / 104 待确认 / 2 原文缺口。七组详细分析中六组成功，第四组两次输出均未覆盖正确引用，报告如实显示 invalid_citations。
- 修复“输出校验失败后原样盲重试”：保留冻结材料和格式合同，为唯一一次输出重试附上应用生成的有界错误代码、叶子分组必须覆盖的来源编号与引用要求。没有向模型提供新的材料，不代写遗漏事实或篡改模型引用。
- 引用反馈回归测试模拟只有收到正确错误反馈后才能恢复的模型，验证两次调用前两条冻结消息相同、仅当前分组重试、初始分析与判断未重跑。JSON 恢复及生命周期 64 项通过，新增反馈用例另 1 项通过。
- 当前报告 #30 重试 #29，复用 184 条成功判断和六组详细分析，仅补失败叶子分组及最终总览。

### 验收完成

最终报告 #31（DeepSeek V4 Pro）已真实生成并在 Chrome 应用页面验证：http://127.0.0.1:5173/reports?report=31 。

- 原始 186 条选材完全保留；184 条可用、2 条原文缺口；49 相关 / 31 无关 / 104 待确认；失败 0。
- 184 条相关性判断、七组详细分析、总览全部 completed。最后一次重试复用所有判断及七组详细分析，仅重新生成总览。
- 七组详细分析引用覆盖全部 49 条相关来源；总览引用均属于这些真实来源，无额外编号。
- 总览 16 段均为完整句子，未出现内部来源编号注释；传承启元府的延期、验收、交付合并为一段并引用两篇原文。
- 在实际报告页面逐段查看，点击楼盘事件原文链接成功打开对应今日头条文章。
- 机器可核验结果保存在本机 runtime/tool-validation/report-generation-chain/acceptance.json，不随代码发布。代码未提交，应用默认模型已保存为 DeepSeek V4 Pro。
- 本轮报告链路验收通过；原有 88 项全量测试失败尚未解决，见 baseline-failures.md，不据此宣称整个项目无缺陷。

用户随后要求提交推送并重置应用业务数据；上面的报告编号和本机链接是重置前的验收记录，重置后不再指向该历史报告。
