# 13: 未适配平台提示与历史兼容

**What to build:** 用户能清楚看到哪些平台尚未接入新采集能力，不能误把不支持当作没有结果。迁移期间历史内容和报告仍可查看，不需要访问平台的已有可用总结仍可选入新报告。

**Blocked by:** 01：采集器替换的前置整理；03：手选已有总结生成报告

**Status:** resolved (offline; final real-site acceptance remains gated by 14)

Labels: ready-for-agent

- [x] 新采集入口与结果展示使用明确的平台能力状态，尚未适配平台不能启动新采集，提示不能伪装为正常空结果。
- [x] 不能执行的请求在工作启动前被识别，不隐式回退到旧采集器，也不访问对应平台账号。
- [x] 旧内容、发现记录、单条处理历史和报告仍可读取，缺失旧字段或旧来源关系时给出真实状态，不伪造新式成功总结。
- [x] 不需要新平台访问的已有可用总结仍能通过手选生成报告；不支持新采集不等于禁止使用历史成果。
- [x] 全库选材涉及尚不能补全的内容时明确保留条目及原因，不静默排除；已有成功总结与需要平台访问的条目分别处理。
- [x] 列表、详情、分页和历史报告读取均无采集、下载或模型副作用。
- [x] 必要的数据迁移保持增量、可回滚且保留原记录，迁移失败不会破坏历史数据。
- [x] 仅约束本轮手动入口的能力与兼容表现，不更改或启动现有自动化；若发现切换影响自动化，记录并交给最终退出前确认。
- [x] 使用跨平台历史样本验证未适配拒绝、只读查看、可用总结复用、缺失字段和迁移失败，全程不连接真实平台。

## 执行边界与验证

本票使新手动入口具有诚实的能力边界，不代表提前删除旧库或默认批量停用现有自动化。05 如已完成则一起验证其全库选材表现；没有 05 时仍可用手选和候选返回完成本票闭环。

常规验收使用本地隔离数据库、固定样本和外部依赖替身，通过用户操作及本机应用接口检查最终状态和副作用；不使用日常运行数据，不访问小红书，不购买采集服务，不修改或启动自动化。实际微博账号验证遵循 14 的限定范围；发布本票不等于已开始实施。

对应父规格验收场景：06、24。通用范围限制对全部任务有效。

## Answer

原生运行时仅声明微博可用。平台连接、单平台搜索、批次启动以及暂停后继续/人工页面/恢复均在准入前检查能力；不会回退旧采集器。界面根据服务端能力禁用未适配平台，能力读取失败时禁止启动。已有跨平台正文和成功总结仍能报告；只有 URL、尚不能补全的条目保留在本次选材并记录 platform_not_supported，而不是伪装成空结果或悄悄排除。

手动界面收敛为内容库勾选/全库待处理后一次开始，保留本次自定义理解与报告指令，成功后定位到进度区。旧独立初步分析按钮移除；旧记录、报告和报告单独重试仍可查看，历史入口折叠展示，现有自动化配置与执行逻辑未改。

11 项新兼容测试通过，覆盖四个未适配平台的历史正文/总结复用、失败项全库选择、只读分页和来源、准入拒绝及暂停时能力变化。相关 163 项既有回归通过；全量后的历史迁移样本修正及相关 35 项通过。前端相关 67 项通过，最后界面微调后相关 49 项、typecheck 和 lint 通过。Chrome 实际页面验证使用临时数据库、合成模型和浏览器替身：成功产出报告、再次选材复用 1 条总结、平台能力禁用及缓存策略保存均可见；没有连接任何真实平台。
