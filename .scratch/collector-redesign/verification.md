# 离线实现验证记录

日期：2026-09-03。范围：01–13；14 的真实微博验收、依赖退出、最终审查和提交均未完成。

## 自动回归

- 后端 `uv run pytest -q`：1,180 项，1,176 通过，4 项失败，耗时 271 秒。失败为历史测试的新增表集合/版本硬编码，以及将最新版库降标为 v15 的不真实样本；未以放宽生产迁移覆盖已有表来通过测试。
- 更新对应历史样本后，`test_collection_schedule_repository.py`、`test_topic_report_migrations.py`、`test_two_stage_prompts.py` 共 35 项通过，包括上述全部失败项。生产迁移保持增量、失败回滚和历史行保留。
- 前端全量 `pnpm run test:run`：32 文件、560 项通过。
- 全量后实际页面检查引出的旧说明、历史折叠、进度焦点和媒体页语义微调，相关 49 项通过；此前平台入口与报告选材相关 67 项通过。最终 typecheck、lint、build 均通过。
- 后端 Ruff 与 `git diff --check` 通过。浏览器、媒体、下载器、模型均使用固定输入/外部依赖替身；短视频检查使用本机既有 ffprobe，不调用真实模型。

## 本机实际页面检查

使用 `/tmp/collector-ui-check.qEkmTt` 下的独立 SQLite 与两个临时本地服务，端口 18142/15174。应用业务代码未替换，外部浏览器/平台网络和模型为测试替身；没有载入日常运行库或实际凭据。

- 从页面配置合成模型、手选合成来源并启动。首轮使用与测试替身要求不符的虚拟密钥，任务真实显示中断；修正临时配置后，通过“全库包含失败项”生成报告。
- 再次选中相同条目，显示本次新总结 0、复用总结 1，直接产出新报告并保留来源。启动后焦点定位到报告任务，显示当前状态与数量。
- 原生模式的手工采集页只有微博可选，其他平台显示“尚未接入”并禁用；只查看页面，没有点击账号连接或真实搜索。
- 媒体策略从默认 30 天/1024 MiB 改为测试用 14 天/512 MiB，保存反馈与界面数值一致；临时库没有原文件，清理返回 0 个文件。
- 没有访问来源链接、操作微博或小红书账号、测试实际模型连接、启动或修改自动任务。桌面页面已查看截图；移动设备/实机媒体理解不计为通过。

## 未完成边界

MediaCrawler 仍是默认采集运行时，显式 `native-weibo` 不会自动回退旧实现。用户 fork 未删除。日常数据库未执行 v20–v24，日常后端仍暂停。所有真实站点兼容性、稳定性及媒体理解结论仍待 14；不能宣称已解决平台风控或完成迁移。最终双轴代码审查与 Git 提交等待基线/提交范围确认，详见实施记录。
