# 小规模自动任务真实验收

日期：2026-09-09（Asia/Shanghai）。使用本机应用、专用浏览器与现有 DeepSeek 配置，未使用采集或模型替身。

## 范围与实际结果

- 独立监控规则 #2：仅“龙田街道”；自动任务 #1：仅微博，每词上限2条。
- 实际采集仅两次，每次2条，共4次命中，去重后新增2条；内容库由原10条变为12条。
- 未提升上限、增加平台或词语，也没有为生成正文扩大采集。

| 运行记录 | 触发 | 结果 | 实际采集 | 模型调用 |
| --- | --- | --- | --- | --- |
| #1 | 页面点击“立即运行”并确认 | completed | 批次 #2，采集运行 #6，新增2条 | 单条总结2次、相关性判断2次 |
| #2 | 修复前真实定时触发 | 停在queued，后取消并保留记录 | 没有采集子任务，0条 | 0次 |
| #3 | 修复后真实定时触发 | completed / no_new_sources | 批次 #3，采集运行 #7，重复2条、新增0条 | 0次 |

## 定时死锁与复验

第一次计划时间为北京时间05:03:36，调度器创建了运行 #2，但执行阶段没有开始。查明 tick 持有同一把锁调用任务启动，启动再次等待该锁而死锁。修复将运行接纳放到计划领取锁之外，并增加真正调用 tick 驱动运行的限时回归。

为验证修复后的真实调度链，而非仅依赖启动恢复，取消并保留未采集的 #2。修复加载后再次等待真实时钟：计划时间北京时间05:10:06.215，采集批次于05:10:06.541创建，于05:10:07.260完成。没有直接调用 tick 或修改计划时间来伪造触发。

发现新定时运行后立即停用后续计划；最终任务 enabled=false、next_due_at=null。运行 #3完成后继续核对，没有额外运行或采集。

## 报告核验与覆盖限制

运行 #1的自动报告 #2已实际在页面打开，并展开材料筛选说明：2条均成功形成单条总结，相关性判断为无关1条、待确认1条，报告状态为empty / no_relevant_sources。页面显示判断原因和两个原帖链接，未把待确认写成无关。

此次没有相关来源，因此没有执行报告正文汇总模型调用。不能据此宣称真实正文汇总已再次通过；本次真实覆盖的是采集、单条总结、相关性判断、空报告交付及定时重复处理。按小规模边界不另增材料凑正文。

运行 #3没有新增成员，不再调用模型；保留零来源空报告记录 #3，未生成正文。

## 文件与运行环境

当前业务库已升级到v38，原报告 #1仍可读取，原有10条内容保留。实际验收未重新创建媒体缓存或临时媒体根目录。53个历史下载媒体已删除，详见媒体清理记录；截图及非媒体资料保留。

详细API快照、报告阅读文本、页面截图保存在本机忽略目录 runtime/tool-validation/media-exit-automation，未写入Git跟踪产物。页面入口：

- 自动任务：http://127.0.0.1:5173/automation-tasks
- 手动触发记录：http://127.0.0.1:5173/automation-runs/1
- 修复后定时记录：http://127.0.0.1:5173/automation-runs/3
- 自动报告：http://127.0.0.1:5173/reports?report=2
