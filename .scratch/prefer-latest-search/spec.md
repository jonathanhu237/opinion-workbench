Status: resolved

# 优先使用平台最新排序

用户决定：平台能拿到最新排序就使用最新；无法启用时继续按平台默认顺序采集。

- 微博保留既有实时搜索。
- 小红书、抖音每个搜索页在采集前尝试网页筛选中的最新排序；等待结果卡片身份或顺序变化，避免读取切换前的旧结果。
- 控件缺失或刷新超时后重新加载原搜索页，继续采集；浏览器断开、登录验证等沿用既有处理。
- 快手、头条保留平台默认顺序。
- 保持每平台每词上限和结果列表展示规则。允许逐词降级，因此运行记录不宣称整个运行保证最新。
- 全部验证在本机完成，不提交 Git。

## 验证

- 针对测试 110 项通过，Ruff 与 diff 空白检查通过。
- 完整后端测试：1097 passed / 90 failed；90 个失败测试与修改前独立基线完全一致，无新增失败。
- 使用当前登录态调用实际 ManagedChrome 排序方法：小红书、抖音均返回成功，结果日期分别从 11 小时前、6 小时前开始倒序排列。
- 本地后端已重新启动加载新代码。
